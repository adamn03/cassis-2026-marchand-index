# Session Handoff
Date: 2026-07-03
Active: NHL_Marchand_Index — FULL BUILD. Branch: `marchand-index-full-build`.

STATUS: working — BUT **UNCOMMITTED WORK IN TREE** (this session's changes are NOT committed/pushed yet; commit is step 1 next session).

THIS SESSION (2026-07-03) — senior-analytics gap review → pre-Reddit fixes implemented while amendment window still open (Reddit creds still absent, 0/774 fetched, so all changes are prereg-clean):

1. **Gap analysis delivered (findings, ranked).** CRITICAL: (a) `fetch_trends.py` single-term payloads = per-player self-normalized shape statistic, NOT cross-player volume + raw-string homonyms ("Will Smith" = actor); (b) Reddit last-name-only query misattributes within-pool shared surnames (Hughes×3, Tkachuk×2, Jones/Smith/Johnson); (c) A1 wiki resolver can pick the WRONG hockey player (suspect: Ryan Johnson MI #1 — retired 1976-born NHLer may hold slug; also Ben Jones/Nathan Walker identical OAQ 1.0207 to 4dp = unexplained, trace it); (d) z-of-raw components star-dominated (engagement_raw max +7.5) → log1p lens needed. HIGH: criterion-2 (≥3 validation pathways) currently failing — only V1b powered; V2 underpowered forever (no ASG 2025/2026); fixes = 2025-26 jersey list (publishes ~June 2026 — CHECK NOW, it's July), run Gate-4 YouTube (~8 fetch-days at free quota, prereg §5–§8 already written), value-prop #2/#6 as insurance arm; V3 mechanical baseline (0.4194) beats OAQ (0.3622) → interpretation had to be pre-declared BEFORE full data (done, A18); MI-hybrid headline is denominator-driven (min-cap vets top it) → headline the poster on OAQ/validation finding or within-cap-tier MI, decide pre-results. MEDIUM (disclose/cheap): reddit 1000-cap censors stars; bootstrap holds Trends/peers/market/expected_cap fixed + iid days (understated CI — disclose or weekly blocks); star-boundary OAQ inflation (no upper peers — add peer-skill-gap diagnostic); cap_quality=low 121/774 triage; spec doc drift (NHL_Marchand_Index.md still shows BDS/IG/old weights); reconcile accepted-abstract claims vs poster scope; Gate-3 event-study feasibility never probed.

2. **Amendments A15–A18 LOGGED** in `Full Project Files/marchand_index/preregistration.md` (inserted before Verification log):
   - **A15** Reddit within-pool surname-collision filter (shared surname = ≥2 pool players, folded; attribution requires first-name-prefix word (≥3 chars, exact if shorter) OR pool-unique "<initial>. <surname>" in title/selftext; else → `ambiguous_mentions`, excluded from counts + bootstrap detail; new cols `ambiguous_mentions`, `surname_shared`).
   - **A16** Trends re-fetch: topic-MID entity queries (pytrends `suggestions()`, first type containing "hockey"; fallback `trends_method=string`) + fixed two-term anchor payload [Brad Marchand topic, player]; `trends_12mo := mean(player)/mean(anchor)`; anchor-zero batch = retry; old trends.csv superseded (git history).
   - **A17** log1p robustness lens: `sign(x)·log1p(|x|)` per component before z-scoring, everything else identical; NON-GATING; status rule: OAQ_portable rank agreement < 0.8 → disagreement ships as finding/limitation.
   - **A18** V3 interpretation pre-declaration: PD verdict = OAQ ρ vs 0.40 only; baseline ≥ OAQ ρ is EXPECTED (outcome rewards total fame), not evidence against construct; no other post-hoc reframing permitted.

3. **Code implemented + tested (84/84 pass, was 61):**
   - `fetch_reddit.py`: A15 — `fold()`, `build_surname_map()`, `make_evidence_check()`, `search_sub(..., evidence=)` returns ambiguous count; main wires collision map; COUNTS_FIELDS + snapshot updated.
   - `fetch_trends.py`: A16 — FULL REWRITE (resolve_topic_mid, fetch_pair, ratio_from_means, per-player snapshot resume). **NOT RUN YET** — re-fetch pending (774 players × ~3 calls, pytrends throttling, expect multi-hour w/ resume).
   - `compute_oaq.py`: A17 — `signed_log1p`, `compute_log_lens`, `rank_agreement`; main computes lens + log-lens external validation (V1b/V2/V3 point est.); results.md "A17 log1p robustness lens" section + results.json `log1p_lens` block; OUT_COLS += 4 log cols.
   - New tests: `tests/test_fetch_reddit_a15.py`, `tests/test_fetch_trends_a16.py`, `tests/test_compute_oaq_log_lens.py`.
   - `audit_wiki_identity.py`: WRITTEN, **NOT RUN** — cross-checks every wikidata_qid via P3522 (NHL.com player ID) vs our nhl_player_id, fallback P569 birth-year vs NHL landing birthDate; writes `raw/wiki_identity_audit.csv`; verdicts ok_nhl_id/bad_nhl_id/ok_dob/bad_dob/unverified. Untested against live API (batching, `sess.cache.contains` guard) — expect minor debugging on first run.

NEXT (exact order):
1. **Commit this session's work** (preregistration.md A15–A18 + fetch_reddit.py + fetch_trends.py + compute_oaq.py + 3 test files + audit_wiki_identity.py + SESSION.md). Tests green: `cd "Full Project Files/marchand_index"; python -m pytest -q` → 84 passed.
2. **Run `python audit_wiki_identity.py`** (~16 wikidata batches + NHL landing calls for no-P3522 rows). If bad_* verdicts → log A19 (mechanical resolver fix: accept only entity whose P3522 contains nhl_player_id, else birth-year check), fix slugs, re-fetch en-wiki for affected rows only. Explicitly check Ryan Johnson, Ian Moore, Max/Ben Jones, Nathan Walker, all case-study 8.
3. **Trends re-fetch** under A16: `python fetch_trends.py` (resumable; snapshot per player).
4. **Retrieve 2025-26 NHL/NHLPA most-popular-jerseys list** (publishes ~June 2026 — browser needed, NHLPA page is JS SPA; per external_outcomes_sources.md, populate JERSEY_LIST in fetch_external_outcomes.py + document source URL, re-run) → strengthens V1b, adds within-window outcome.
5. **Reddit track** (unchanged from before, still blocked on creds): add OAuth creds to `marchand_index/.env` → purge `raw/reddit_counts.csv` + `raw/reddit_detail.csv` → `python fetch_reddit.py` (now A15-filtered) → re-fetch intl once (`fetch_wikipedia_intl.py`, populates #2 intl_match flag) → ONE `compute_oaq.py` regen (now also emits A17 log lens) + both `diagnostics/*.py`.
6. **Decide Gate-4 YouTube in/out for poster** (criterion-2 insurance; ~8 fetch-days at 10k units/day free quota; prereg §5–§8 in docs/preregistration.md already locked). Also: pick poster headline strategy (OAQ/validation finding, not raw MI-hybrid leaderboard), spec-drift cleanup pass on NHL_Marchand_Index.md, abstract↔poster claim diff.

CARRY-FORWARD (still valid): 774 locked pool (497F/277D, snapshot 2026-06-17); MoneyPuck cached `raw/moneypuck_skaters_2025.csv`; compute_oaq deterministic (seed 20260526, 1000 draws, ~2-3 min); scrapes share one sqlite http_cache → run fetchers SEQUENTIALLY; UTF-8 forced in _common, console cp1252; YouTube = Gate-4 validation ONLY (never composite input); value-prop backlog #1–#7 in value_propositions.md (build AFTER data + gates); Reddit-NULL verdicts non-final (PA/PB inconclusive, PC confirmed, PD disconfirmed 0.3622<0.40 — all shift post-Reddit; A18 now governs PD interpretation).

Deadline: poster session 2026-09-12 (~10 wk runway).
