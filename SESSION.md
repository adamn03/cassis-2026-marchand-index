# Session Handoff
Date: 2026-07-13
Active: NHL_Marchand_Index — FULL BUILD. Branch: `marchand-index-full-build`.

LAST: OWNER APPROVED ALL DECISIONS (D-1 GO+U1, D-2 rebuild primary, D-3 sign-off+U2, U-slate U1–U7 + U8-conditional, POOL DEDUP 774→771 as A41). Recorded in impl prereg §14 decision record + decision sheet boxes checked (commit 91ab66b). Amendment batch STARTED, executing `2026-07-12-amendment-proposals.md` in order, each text commit BEFORE its code commit:
- A24 DONE (text f8d67e7, code a966fc7): CapWages field discovery ran — key path `props.pageProps.player.contracts[i].type` on the contract whose details[] holds season=="2025-26"; `is_rookie_deal` = "entry-level" substring; `rookie_flag_source` col; expected_cap = log-fit on non-rookie rows + Duan smearing; linear all-rows audit lens col.
- A25 DONE (e4449cf, cdb4c04): `classify_null_reasons()` + `apply_null_taxonomy()` in compute_oaq (wiki_match=none / intl_match=none / no query_mid → no_entity_exists → raw 0 pre-zscore; else fetch_failed → renorm unchanged); `null_reasons` col; results.md listing.
- A26 DONE (e5f96c7, 0f55ea0): `block_resample()` 7-day circular blocks for wiki en+intl daily vectors in bootstrap; propagation table in results.md.
- A27 TEXT COMMITTED (2ddb75b); CODE IN WORKING TREE, UNCOMMITTED: compute_oaq.py has `compute_boundary_bias()` (peer_skill_gap ×6 + scalar, OAQ_bc/OAQ_portable_bc, β̂ within-position, wired into compute_oaq() + OUT_COLS + results.md A27 section); new tests/test_boundary_bias_a27.py. Last full run: 144 pass / 1 fail; the failing test was then split into linear-surface (exact removal) + convex-surface (attenuation <0.6×) variants — NOT re-run since. Session ended mid-verify.

STATUS: working (repo clean through A26; A27 code dirty + unverified)

NEXT: 1) From `Full Project Files/marchand_index/`: `pytest -q` → expect ~146 green (if the convex threshold still fails, loosen to <0.7× — linear-exactness test is the load-bearing one) → commit A27 code: `marchand_index: A27 code + tests — peer_skill_gap + bias-corrected lens`. 2) Resume corpus pull `python fetch_reddit_corpus.py` (died with session; 15/36 subs done through NewYorkIslanders.part; resumable, skips finished). 3) Re-run `python fetch_cap_hits.py` (A24 contract_type re-fetch died mid-run ~row 300; atomic write means old CSV intact; HTTP cache makes re-run fast) → commit raw/cap_hits.csv as `marchand_index: A24 cap re-fetch with contract_type`. 4) Continue batch IN ORDER per task list: A28 → A29 (+1.9 re-fetch) → A30 (probe subscriber transport first: Arctic Shift subreddits endpoint or www.reddit.com/r/<sub>/about.json — supplement §5; then write text per D-2 rebuild) → A31 (write with free-data Task-5 pre-chew rows 3–7 + BH pins + MWU clause + U2 power statement + paired ΔAUC — U2 window closes at A31 commit) → A32 (KEEP D-2 conditional clauses) → A33 (needs web research: 2022/2023/2024 fan-vote press releases ≥2 URLs/season) → A34 → A35 → G4-A1..3 into docs/preregistration.md §11 (G4-A series, NOT bare A-numbers; spec seed 20260522 stays) → A36 (+full 774 wiki augment run) → A37 (web sweep) → A38 (mover list + date research) → A39 → A40 (idea-max §4 draft) → A41 pool dedup (Andrae 499/637, Benoit 500/638, Colton 152/368; inspect rows, mechanical keep rule, 774→771). 5) ONLY after ALL texts committed + corpus done: `python diagnostics/corpus_integrity_scan.py` (copied into repo, untracked) → `python fetch_reddit.py` → dry-load compute_oaq.load_inputs(). NO production compute_oaq (that waits for Phase-1 hygiene + Gate-4 per §E).

GOVERNING RULE REMINDER: every amendment text must land BEFORE `fetch_reddit.py` writes reddit_counts.csv — texts claim "Reddit is 0/774". Corpus jsonl cache ≠ production counts; pulling corpus is fine.

OWNER (2 items left): (a) eyeball `marchand_index/raw/reddit_identity_pairs.md`; (d) YouTube API key — free, ~10 min, Google Cloud Console → enables U1 dry-run + Gate-4 launch.

CARRY-FORWARD: tests 141 committed (146 expected after A27); locked pool 774 (771 after A41); window [2025-04-18, 2026-04-17]; A12 weights unchanged; seed 20260526 (spec prereg 20260522 — never harmonize); corpus in `marchand_index/cache/reddit_corpus/` GITIGNORED LOCAL SOURCE OF RECORD; harness task list has the full batch as tasks #6–#16; source docs: airtight plan v1.1 + 2026-07-12 proposals (A24–A35, G4-A) + free-data supplement (A36/A37 + A31 pre-chew Task 5) + cross-domain supplement (A38/A39) + idea-max §4 (A40 draft) + arctic-shift supplement (§4b dupes, §5 A30 transport).

Deadline: poster 2026-09-12 (~8.5 wk).
